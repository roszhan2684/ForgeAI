# Database (Neon PostgreSQL recommended — https://neon.tech)
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

# AI Providers (at least one required; fallback priority: Anthropic → Gemini → OpenAI → mock)
ANTHROPIC_API_KEY="sk-ant-..."  # optional
GOOGLE_API_KEY="AIza..."        # optional — free tier at aistudio.google.com
OPENAI_API_KEY="sk-..."         # optional

# App
NEXT_PUBLIC_APP_URL="https://your-app.vercel.app"
